@page "/duplicate-session"

<PageTitle>Duplicate session key</PageTitle>

<h1>Duplicate session key</h1>
<p>Rendering two active components that claim <code>validation-session-duplicate</code>. This request is expected to throw.</p>

<SessionDuplicateReader />
<SessionDuplicateReader />